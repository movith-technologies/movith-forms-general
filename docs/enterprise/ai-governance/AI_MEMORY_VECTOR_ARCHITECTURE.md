# AI Memory / Vector Architecture

Movith Forms does not require a core vector memory architecture.

If implemented in future:

- Must be tenant-isolated
- Must not store cross-tenant data
- Must not store sensitive submission content unencrypted
- Must not replace structured relational storage

Vector storage is optional and must remain auxiliary.